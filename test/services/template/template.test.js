import test from 'ava';
import TemplateService from '../../../src/services/template';

test('_defaults', function(t){
  // Lets make sure the defaults are what I think they are
  // helps keep track of changes to the configurationg
  // const defaults = TemplateService._defaults();
  t.pass();
});
